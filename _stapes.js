/**
 * npm init (set up node.js packages)
 * npm install express
 * npm i cors mongodb dotenv
 * to to package.json and  set scripts > start node index.js and "start-dev": "nodemon index.js"
 * cors middleware set up allow to connect server site to clint site (app.use(cors()))
 * port set up (const port = process.env.PORT || 5000;)
 * require('dotenv').config() / secure data
 * body data show to middleware setup (app.use(express.JSON()))
 * create mongoDB database atlas and create data file and insert json data
 * go to connect and copy code and past index.js
 * create new user and .env(.env file create root folder) file (KEY_USER=username) and put this index.js mongoBD code path using template string
 * create a async function run and return run()
 * try{}finally{} set and try into set await clint.connection() then const serviceCollection = client.db("carservice").collection("services");
 * apply get function and set data link and send data clint site
 * fetch data clint site and show display data
 * post data apply app.post("/file",(req,res)) use req.body
 */
